# B1-3 验证记录（2026-03-27）

## 1. 验证目标
- 验证缓存回退策略（Redis 不可用时回退内存）可工作。
- 验证热力层接口缓存命中与统计接口。
- 验证异步任务提交与状态查询链路。

## 2. 验证命令

```powershell
cd d:\小桌面\交通拥堵
$env:PYTHONPATH='.'
$env:PYTHONIOENCODING='utf-8'
python backend/export_openapi.py
```

```powershell
cd d:\小桌面\交通拥堵
$env:PYTHONPATH='.'
@'
# 使用 FastAPI TestClient 执行登录、热力层缓存、cache stats、异步任务提交/查询
'@ | python -
```

## 3. 验证结果
- OpenAPI 导出：成功。
- 接口 smoke：`11/11 PASS`。

关键检查项：
- `GET /api/v1/map/layers/heat` 首次与二次访问均 `200`。
- `GET /api/v1/cache/stats`（admin）返回 `200`，且 `hits>=1`。
- `GET /api/v1/cache/stats`（viewer）返回 `403`。
- `POST /api/v1/tasks/cache/refresh-heat` 返回 `200` 并返回 `task_id`。
- `GET /api/v1/tasks/{task_id}` 返回 `200`，任务状态到达 `completed`。

示例缓存状态（本次执行）：
- `backend=memory`
- `configured_backend=auto`
- `hits=1, misses=1`
- `fallback_reason=redis_unavailable: No module named 'redis'`

## 4. 结论
- 结果：`PASS`
- B1-3 缓存与异步任务机制满足阶段验收要求。

## 5. 签字
- 验证人：`Test-Agent`
- 复核人：`Backend-Agent`
- 日期：`2026-03-27`

