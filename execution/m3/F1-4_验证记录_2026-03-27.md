# F1-4 验证记录（2026-03-27）

## 1. 验证目标
- 验证地图、图表、筛选器联动链路可执行。
- 验证联动状态面板与日志留痕有效。

## 2. 验证方法
- 使用前端结构+接口 smoke（`frontend/m3_f1_smoke.json`）。
- 人工交互路径校验（地图点击、图表点击、事件表点击、筛选切换）。

## 3. 验证结果
- 结构与接口 smoke：`20/20 PASS`
- 交互链路校验：`PASS`

关键检查项：
- 地图点击路段后，`segmentSelect` 与联动状态同步更新。
- 点击事件表行后，`eventSelect` 与 `segmentSelect` 同步更新。
- 点击图表（趋势/瀑布/箱线/回放热图）后，触发路段联动刷新。
- 联动日志可在管理页运行日志看到来源标记。

## 4. 证据路径
- `frontend/m3_f1_smoke.json`
- `frontend/index.html`
- `frontend/assets/app.js`

## 5. 结论
- 结果：`PASS`
- F1-4 联动能力满足阶段验收要求。

