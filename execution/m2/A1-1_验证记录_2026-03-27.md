# A1-1 验证记录（2026-03-27）

## 1. 验证目标
- 验证 `model/baseline/scripts/train_baseline.py` 能否加载 `data-pipeline/features/output/feature_table.csv` 并完成训练/评估。
- 验证输出的 `metrics.json`、`predictions.csv` 与 `model_report.md` 包含预期的指标与依赖说明。
- 验证脚本在 `xgboost` 缺失时能自动回退至 `sklearn.GradientBoostingRegressor`。

## 2. 验证命令
```powershell
python model/baseline/scripts/train_baseline.py
```

## 3. 验证结果
- 命令退出码：`0`，脚本正常完成。
- 输出文件：
  - `model/baseline/output/metrics.json`
  - `model/baseline/output/predictions.csv`
  - `model/baseline/output/model_report.md`
- 树模型指标：`RMSE=10872.84`，`MAE=8442.53`，`MAPE=99.43%`（使用 `sklearn.GradientBoostingRegressor` 回退）。
- ARIMA 指标：`RMSE=13163.50`，`MAE=10684.12`，`MAPE=119.10%`（基于聚合 `metric_sum` 时间序列）。
- `predictions.csv` 验证集：树模型对 3 条验证行（`SEG-4001/SEG-4002/SEG-4003`）给出 ~24 的预测，聚合 ARIMA 对应两个窗口的预测分别为 -373.55 与 5194.68，反映当前样本量极小导致拟合不稳定。
- 脚本执行过程中 statsmodels 依赖会提示“缺乏频率”与“观测过少”的警告，属于预期，未阻止训练。

## 4. 结论
- 结果：`PASS`
- 说明：基线脚本运行正常，结果写入预期文件，同时在文档中记录了 xgboost 缺失时的回退路径，可进入下一阶段模型对比。

## 5. 问题与修复
- 问题：当前样本量少导致 ARIMA 与树模型预测值与实际差异巨大，以及 statsmodels 命令行警告。
- 修复：该阶段记录为“现象”，后续版本可通过扩充样本或做 cross-validation 改善，也可明确提示警告安全可忽略。

## 6. 签字
- 验证人：`Model-Agent`
- 复核人：`Data-Agent`
- 日期：`2026-03-27`
