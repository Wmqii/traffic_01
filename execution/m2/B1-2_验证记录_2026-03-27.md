# B1-2 验证记录（2026-03-27）

## 1. 验证目标
- 验证 JWT 登录、角色权限拦截、OpenAPI 导出能力。

## 2. 验证命令

```powershell
cd d:\小桌面\交通拥堵
$env:PYTHONPATH='.'
$env:PYTHONIOENCODING='utf-8'
python .\backend\export_openapi.py
```

```powershell
cd d:\小桌面\交通拥堵
$env:PYTHONPATH='.'
$env:PYTHONIOENCODING='utf-8'
@'
import os, subprocess, time, json
from urllib import request, error
# 脚本内启动 uvicorn 并验证 login/接口授权（省略）
'@ | python -
```

## 3. 验证结果
- OpenAPI 导出：成功。
- 登录与权限验证结果：
  - `login_viewer = 200`
  - `predictions_with_viewer = 200`
  - `admin_audit_with_viewer = 403`
  - `login_admin = 200`
  - `admin_audit_with_admin = 200`

## 4. 结论
- 结果：`PASS`
- JWT + RBAC 能力满足 B1-2 验收要求。

## 5. 签字
- 验证人：Test-Agent
- 复核人：Backend-Agent
- 日期：2026-03-27
