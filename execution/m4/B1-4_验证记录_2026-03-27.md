# B1-4 验证记录（2026-03-27）

## 1. 验证目标
- 验证 B1-4 调度接口（重训、发布、健康检查）可执行。
- 验证 RBAC 边界生效（viewer 禁止，analyst/admin 按角色可用）。
- 验证模型注册表写入与 active 版本切换成功。

## 2. 验证命令

```powershell
cd d:\小桌面\交通拥堵
$env:PYTHONPATH='.'
python backend/tests/b1_4_scheduler_runner.py
```

## 3. 验证结果
- 控制台摘要：`{"pass": true, "total": 23, "passed": 23, "failed": 0}`
- 关键结果：
  - 重训任务提交并完成，产出候选：`gru-<timestamp>`
  - 发布任务提交并完成，active 切换成功并与候选一致
  - 同步健康检查：`healthy`
  - 异步健康检查任务：`completed`
  - registry 查询：active 版本与发布结果一致
- 结论：`PASS`

## 4. 证据路径
- `backend/tests/output/b1_4_scheduler_result.json`
- `model/registry/model_registry.json`
- `backend/openapi.json`
- `backend/tests/b1_4_scheduler_runner.py`

## 5. 结论
- B1-4 调度接口验证通过，可进入 B1-5（审计日志与错误码规范）阶段。
