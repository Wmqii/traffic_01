# D1-2 验证记录（2026-03-27）

## 1. 验证目标
- 验证数据治理规则脚本的可执行性与规则覆盖完整性。
- 验证治理输出（clean/reject/report）是否正确生成。

## 2. 验证命令

```powershell
cd d:\小桌面\交通拥堵\data-pipeline\governance
python .\scripts\governance_runner.py
python .\scripts\governance_runner.py --demo-anomalies
```

## 3. 验证结果
- 命令执行均成功，返回：
  - `Governance completed (normal). Sources processed: 4.`
  - `Governance completed (demo). Sources processed: 4.`
- 输出文件存在：
  - `output/clean/*.csv`
  - `output/rejects/*_rejects.csv`
  - `output/governance_report_normal.json`
  - `output/governance_report_demo.json`
- demo 快照结果验证：
  - 缺失值检测：`output/rejects_demo/taxi_rejects.csv` 命中 `missing:passenger_count`
  - 重复值检测：`output/rejects_demo/taxi_rejects.csv` 命中 `duplicate:key`
  - 异常值检测：`output/rejects_demo/metro_rejects.csv` 命中 `anomaly:occupancy_pct`
  - 延迟检测：`output/rejects_demo/weather_rejects.csv` 命中 `delay:late_record`

## 4. 结论
- 结果：`PASS`
- D1-2 治理规则已可运行并覆盖目标四类问题，允许任务进入评审收口。

## 5. 签字
- 验证人：Test-Agent
- 复核人：Data-Agent
- 日期：2026-03-27
