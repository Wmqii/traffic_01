# F1-5 验证记录（2026-03-27）

## 1. 验证目标
- 验证导出中心 UI 已接入并可触发导出逻辑。
- 验证 PNG/CSV/Markdown 三类导出能力代码路径完整。

## 2. 验证命令

```powershell
cd d:\小桌面\交通拥堵
node -e "const fs=require('fs'); const src=fs.readFileSync('frontend/assets/app.js','utf8'); new Function(src); console.log('app.js syntax ok');"
```

```powershell
cd d:\小桌面\交通拥堵
$env:PYTHONPATH='.'
$env:PYTHONIOENCODING='utf-8'
# 生成 F1-5 smoke 证据（HTML/JS 导出能力静态检查 + API基础检查）
@'
# ...省略，执行后输出 frontend/m3_f1_5_smoke.json
'@ | python -
```

## 3. 验证结果
- 语法检查：`app.js syntax ok`
- Smoke：`frontend/m3_f1_5_smoke.json` 显示 `PASS`

关键检查项：
- HTML 中存在导出中心元素与按钮：
  - `exportChartSelect`
  - `exportChartBtn`
  - `exportEventsCsvBtn`
  - `exportHeatCsvBtn`
  - `exportFeaturesCsvBtn`
  - `exportAttributionCsvBtn`
  - `exportBriefBtn`
- JS 中存在导出函数：
  - `exportChartPng`
  - `exportCsvByType`
  - `exportBriefMarkdown`
  - `downloadDataUrl`
  - `downloadText`

## 4. 证据路径
- `frontend/m3_f1_5_smoke.json`
- `frontend/index.html`
- `frontend/assets/app.js`
- `frontend/README.md`

## 5. 结论
- 结果：`PASS`
- F1-5 导出能力满足阶段交付要求。

