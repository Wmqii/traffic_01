# A1-6 验证记录（2026-03-27）

## 1. 验证目标
- 验证模型版本管理能力（重训、发布、回滚）可执行。
- 验证回滚后 active 版本正确恢复。
- 验证 registry 历史可追踪 rollback 事件，且健康状态正常。

## 2. 验证命令

```powershell
cd d:\小桌面\交通拥堵
$env:PYTHONPATH='.'
python backend/tests/a1_6_model_rollback_runner.py
```

## 3. 验证结果
- 控制台摘要：`{"pass": true, "total": 30, "passed": 30, "failed": 0}`
- 关键结果：
  - v1/v2 候选训练与发布成功
  - active 从 v1 切换到 v2 后，按目标版本回滚到 v1 成功
  - registry `history` 存在 rollback 事件
  - 回滚后 `model health=healthy`
- 结论：`PASS`

## 4. 证据路径
- `model/registry/a1_6_rollback_result.json`
- `model/registry/model_registry.json`
- `backend/openapi.json`
- `backend/tests/a1_6_model_rollback_runner.py`

## 5. 结论
- A1-6 模型版本管理与回滚策略验证通过，M4 启动批次任务全部完成。

